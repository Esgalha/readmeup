name: CI (docs)

on:
  pull_request:
    paths:
      - '**.md'
      - 'screenshots/**'
      - '.github/dependabot.yml'

jobs:
  ci:
    name: Lint, type-check, test
    runs-on: ubuntu-latest
    steps:
      - run: echo "Documentation-only change, skipping CI"
